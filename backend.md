### 0:00-0:30 — ВВЕДЕНИЕ (30 секунд)

> **Привет, меня зовут [Имя].**
>
> Я работал над **backend-частью** Pool CRM — файлом `main.py`, который является сердцем нашего приложения. Это веб-сервер на **FastAPI**, который обрабатывает все запросы, работает с базой данных и общается с фронтендом.
>
> Мой код отвечает за: аутентификацию, ролевую модель, управление детьми, группами, тренерами, тренировками, посещаемостью, заявками, переводами, галереей и интеграцию с VK ботом.

---

### 0:30-1:15 — АРХИТЕКТУРА И ТЕХНОЛОГИИ (45 секунд)

> **Технологический стек:**
> - **FastAPI** — современный асинхронный фреймворк на Python
> - **SQLAlchemy** — ORM для работы с базой данных
> - **Jinja2** — шаблонизатор для HTML
> - **SQLite** — лёгкая база данных
>
> **Как это работает:**
> ```
> Браузер → FastAPI (main.py) → База данных → HTML ответ
>          ↓
>     VK Бот (vk_bot.py)
> ```
>

---

### 1:15-2:00 — КЛЮЧЕВЫЕ ОСОБЕННОСТИ (45 секунд)

> **Первая особенность — ролевая модель.**
> У нас три роли: `admin`, `coach`, `parent`. От роли зависит, какие данные видит пользователь.
>
> **Вторая особенность — VK интеграция.**
> Родители могут привязать VK аккаунт через 6-значный код. После этого они получают уведомления о посещаемости детей и могут командой `дети` узнать, в какой группе занимается ребёнок.

---

### 2:00-2:45 — ТРУДНОСТИ И РЕШЕНИЯ (45 секунд)

> **Первая трудность — каскадное удаление.**
> При удалении тренера мы должны очистить его группы и расписание. Решение: создал отдельные функции `delete_coach_cascade()`, `delete_group_cascade()`, которые удаляют все связанные данные.
>
> **Вторая трудность — дублирование кода в шаблонах.**
> Раньше в каждом HTML-файле была своя шапка и меню — 15 копий одного и того же. Решение: перенёс всё в `base.html`, а в дочерних шаблонах оставил только уникальный контент. Сократил код на **500 строк**.
>
> **Третья трудность — VK бот.**
> Пришлось разобраться с Long Poll API, правильно настроить сервер и обработку сообщений.

---

### 2:45-3:15 — УСПЕХИ И ДОСТИЖЕНИЯ (30 секунд)

> **Что мне удалось лучше всего:**
>
> 2. **Полная поддержка каскадного удаления** — при удалении тренера очищаются все его группы и расписание, не оставляя "битых" ссылок.
---

### 3:15-4:00 — ВЗАИМОДЕЙСТВИЕ С КОМАНДОЙ (45 секунд)

> **Как мы работали:**
>
> - **GitHub** — все изменения через pull requests
> - **Код-ревью** — проверяли код друг друга перед слиянием
>
> **Моя роль в команде:**
>
> - Отвечал за backend и структуру `main.py`
> - Помогал фронтендерам с шаблонами и API
> - Настроил VK бота и интеграцию с базой данных
> - Писал функции проверки прав доступа
>
> **Конфликты и их решение:**
>
> Была ситуация, когда шапка и меню дублировались в каждом файле. Я предложил вынести всё в `base.html`, и мы приняли это решение. Это сэкономило около **500 строк кода** и упростило поддержку.

---

### 4:00-4:30 — ДЕМО (30 секунд)

> **Сейчас я покажу, как работает наше приложение:**
>
> 1. Вход как администратор — вижу дашборд со статистикой.
> 2. Просмотр заявок — одобряю новую заявку на зачисление.
> 3. Отметка посещаемости как тренер.
> 4. Генерация VK кода как родитель.
> 5. Фотогалерея — загрузка и просмотр фото.
> 6. Страница 404 с дельфином 🐬

---

### 4:30-5:00 — ИТОГИ И ПЛАНЫ (30 секунд)

> **Что сделано:**
>
> - ✅ Полноценный backend на FastAPI
> - ✅ Рабочий VK бот с командами
> - ✅ 15+ HTML шаблонов
> - ✅ REST API для будущих интеграций
> - ✅ Каскадное удаление всех сущностей
> - ✅ Страница 404 с дельфином 🐬
>
> **Планы на будущее:**
>
> - 🔜 Переход на PostgreSQL
> - 🔜 Хэширование паролей (bcrypt)
> - 🔜 JWT аутентификация
> - 🔜 Экспорт отчётов в Excel
> - 🔜 Онлайн-оплата абонементов
>
> **Спасибо за внимание! Вопросы?** 🎤

---


https://vk.com/im/convo/-238052616

## 📝 ЧАСТЬ 1: ИСПРАВЛЕННЫЙ README.md

```markdown
# 🐬 Pool CRM - Система управления плавательным бассейном

Полноценная CRM-система для управления детскими спортивными группами, расписанием, посещаемостью и коммуникацией с родителями через VK бота.

## ✨ Возможности

### 👑 Администратор
- Управление группами, тренерами, детьми
- Обработка заявок на зачисление (одобрение/отклонение)
- Управление переводами между группами
- Ведение фотогалереи бассейна и тренеров
- Контроль посещаемости
- Генерация расписания тренировок

### 🏊 Тренер
- Просмотр своих групп
- Отметка посещаемости на тренировках
- Создание запросов на перевод детей (в т.ч. зачисление новых детей без группы)
- Просмотр расписания

### 👨‍👩‍👧 Родитель
- Добавление/редактирование данных детей
- Просмотр расписания и посещаемости
- Привязка VK аккаунта для уведомлений
- Подача заявки на зачисление (даже без регистрации)

### 🤖 VK Бот
Команды для привязанных родителей:
- `дети` - список детей с группами
- `сегодня` - сегодняшние тренировки
- `завтра` - завтрашние тренировки
- `неделя` - тренировки на неделю
- `уведомления вкл/выкл` - управление оповещениями
- `помощь` - справка

## 🚀 Быстрый старт

### 1. Клонирование и установка
```bash
git clone https://github.com/your/pool-crm.git
cd pool-crm
pip install -r requirements.txt
```

### 2. Настройка окружения
```bash
cp .env.example .env
# Отредактируйте .env (VK токен, секретный ключ)
```

### 3. Инициализация БД
```bash
python seed_database.py   # Заполнение тестовыми данными
```

### 4. Запуск
```bash
python main.py
# Или
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 5. Доступ к системе
- **Веб-интерфейс:** http://127.0.0.1:8000

## 🔑 Тестовые учётные записи

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@pool.ru | admin123 |
| Родитель | maria@example.com | parent123 |
| Родитель | sergey@example.com | parent123 |
| Родитель | anna@example.com | parent123 |
| Тренер | ivan.coach@pool.ru | coach123 |
| Тренер | elena.coach@pool.ru | coach123 |
| Тренер | alexey.coach@pool.ru | coach123 |

## 📁 Структура проекта

```
pool-crm/
├── main.py                 # 🌐 Веб-приложение (FastAPI + Jinja2) - ОСНОВНОЙ ФАЙЛ
├── database.py             # 🗄️ Модели БД (SQLAlchemy + SQLite)
├── vk_bot.py               # 🤖 VK Long Poll бот
├── seed_database.py        # 🌱 Заполнение тестовыми данными
├── requirements.txt        # 📦 Зависимости Python
├── .env                    # 🔐 Переменные окружения (токены)
├── static/
│   ├── style.css           # 🎨 Основные стили
│   ├── bubbles.js         � # 🫧 Анимация пузырьков
│   └── uploads/gallery/    # 🖼️ Загруженные фото
├── templates/              # 📄 HTML шаблоны (Jinja2)
│   ├── base.html           # Базовый шаблон
│   ├── dashboard.html      # Дашборд
│   ├── children.html       # Список детей
│   ├── child_form.html     # Форма ребёнка
│   ├── groups.html         # Список групп
│   ├── group_form.html     # Форма группы
│   ├── trainers.html       # Список тренеров
│   ├── trainer_form.html   # Форма тренера
│   ├── trainings.html      # Список тренировок
│   ├── attendance.html     # Посещаемость
│   ├── applications.html   # Заявки на зачисление
│   ├── application_detail.html # Детали заявки
│   ├── transfers.html      # Запросы на перевод
│   ├── transfer_form.html  # Форма запроса на перевод
│   ├── gallery.html        # Фотогалерея
│   ├── profile.html        # Профиль родителя
│   ├── login.html          # Страница входа
│   ├── register.html       # Подача заявки
│   └── index.html          # Главная страница
└── pool_crm.db             # 💾 SQLite база данных
```

## 🛠️ Технологии

| Компонент | Технология |
|-----------|------------|
| Backend | FastAPI (Python 3.10+) |
| ORM | SQLAlchemy 2.0+ |
| База данных | SQLite |
| Шаблоны | Jinja2 |
| VK API | requests + Long Poll |
| Фронтенд | HTML5, CSS3 (Flexbox/Grid), Vanilla JS |
| Анимации | CSS Keyframes |

---

# 📡 ПОЛНЫЙ СПИСОК ЭНДПОИНТОВ (main.py)

## 🔐 Аутентификация

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/` | Главная страница с выбором входа/регистрации | Все |
| GET | `/login` | Страница входа | Гости |
| POST | `/login` | Обработка входа (email/пароль). Создаёт сессию | Гости |
| GET | `/logout` | Выход, удаление сессии | Авторизованные |
| GET | `/register` | Страница подачи заявки на зачисление | Гости |
| POST | `/register` | Отправка заявки. Создаёт запись в `applications` | Гости |

**Детали авторизации:**
- Поддерживаются 3 роли: `admin`, `parent`, `coach`
- Сессии хранятся в словаре `sessions` с cookie `session_id`
- Админ: admin@pool.ru / admin123 (хардкод в login)
- Родители/тренеры: проверка по `ParentDB` / `CoachDB`

---

## 📊 Дашборд

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/dashboard` | Панель управления со статистикой. Для родителя - список его детей | Все роли |

**Возвращаемая статистика:**
- Количество детей, групп, тренеров
- Тренировок в текущем месяце
- Процент посещаемости
- Количество заявок на рассмотрении

---

## 👶 Управление детьми

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/children` | Список детей. Для родителя - только его дети | Все |
| GET | `/children/add` | Форма добавления ребёнка (с выбором группы) | Parent |
| POST | `/children/add` | Создание ребёнка. Можно сразу зачислить в группу | Parent |
| GET | `/children/{child_id}/edit` | Форма редактирования ребёнка | Parent |
| POST | `/children/{child_id}/edit` | Обновление данных ребёнка | Parent |
| GET | `/children/{child_id}/delete` | Деактивация ребёнка (soft delete) | Admin |
| POST | `/children/{child_id}/enroll` | Зачисление ребёнка в группу | Admin, Coach |
| POST | `/children/{child_id}/disenroll` | Отчисление из группы | Admin, Coach |

---

## 👥 Управление группами

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/groups` | Список групп. Для тренера - только его группы | Все |
| GET | `/groups/add` | Форма создания группы | Admin |
| POST | `/groups/add` | Создание новой группы | Admin |
| GET | `/groups/{group_id}/edit` | Форма редактирования | Admin |
| POST | `/groups/{group_id}/edit` | Обновление группы | Admin |
| GET | `/groups/{group_id}/delete` | Деактивация группы | Admin |

---

## 🏊 Управление тренерами

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/trainers` | Список всех тренеров | Admin |
| GET | `/trainers/add` | Форма добавления тренера | Admin |
| POST | `/trainers/add` | Создание тренера (хэш пароля в открытом виде) | Admin |
| GET | `/trainers/{coach_id}/edit` | Форма редактирования | Admin |
| POST | `/trainers/{coach_id}/edit` | Обновление тренера | Admin |
| GET | `/trainers/{coach_id}/delete` | Деактивация тренера (soft delete) | Admin |

---

## 📅 Тренировки

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/trainings` | Список тренировок с фильтрацией по группе и статусу | Все |
| GET | `/trainings/generate` | Генерация тренировок на месяц (по расписанию групп) | Admin |

**Параметры фильтрации GET `/trainings`:**
- `group_id` - ID группы
- `status` - scheduled / completed / cancelled

---

## ✅ Посещаемость

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/attendance` | Страница выбора тренировки и просмотра/отметки посещаемости | Все |
| POST | `/attendance/save` | Сохранение отметок (массовое обновление) | Admin, Coach |

**Статусы посещаемости:**
- `present` - присутствовал
- `absent_sick` - болезнь
- `absent_family` - семейные обстоятельства
- `absent_no_reason` - без уважительной причины

**Особенности:**
- Для родителя - только его дети
- При отметке автоматически отправляется VK уведомление родителю (если привязан и включены уведомления)

---

## 📝 Заявки на зачисление (Admin)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/applications` | Список заявок с фильтром по статусу |
| GET | `/applications/{app_id}` | Детальный просмотр заявки |
| GET | `/applications/{app_id}/approve` | ✅ Одобрение. Создаёт родителя, ребёнка, зачисление |
| GET | `/applications/{app_id}/reject` | ❌ Отклонение |
| GET | `/applications/{app_id}/waiting` | 📋 Перевод в лист ожидания |
| POST | `/applications/{app_id}/comment` | Добавление комментария администратора |

**Статусы заявок:**
- `new` - новая
- `on_review` - на рассмотрении
- `approved` - одобрена
- `rejected` - отклонена
- `waiting_list` - лист ожидания

---

## 🔄 Переводы между группами

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/transfers` | Список запросов на перевод | Admin, Coach |
| GET | `/transfers/add` | Форма создания запроса (выбор ребёнка, группы) | Coach |
| POST | `/transfers/add` | Создание запроса | Coach |
| GET | `/transfers/{id}/approve` | Одобрение перевода (создаёт историю) | Admin |
| GET | `/transfers/{id}/reject` | Отклонение перевода | Admin |

**Типы переводов:**
1. **Перевод из группы в группу** - у ребёнка есть текущая группа
2. **Новое зачисление** - ребёнок без группы (тренер предлагает зачислить)

---

## 👤 Профиль родителя

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/profile` | Просмотр профиля. Генерирует 6-значный код для VK |
| POST | `/profile` | Обновление имени, email, телефона, пароля, настроек уведомлений |
| GET | `/profile/unlink-vk` | Отвязка VK аккаунта |

**Настройки уведомлений:**
- `notify_absences` - уведомления о пропусках
- `notify_reminders` - уведомления-напоминания

---

## 🖼️ Галерея

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/gallery` | Просмотр галереи (фильтр по типу: бассейн/тренеры) | Все |
| POST | `/gallery/upload` | Загрузка фото (создание миниатюры) | Admin |
| POST | `/gallery/{id}/delete` | Удаление фото и файлов | Admin |
| POST | `/gallery/{id}/reorder` | Изменение порядка сортировки | Admin |

---

## 🔌 API для VK интеграции

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/parents/{parent_id}/generate-vk-code` | Генерация 6-значного кода для привязки VK (действует 10 минут) |

**Процесс привязки VK:**
1. Родитель заходит в `/profile` и нажимает "Сгенерировать код"
2. Пишет боту: `привязать КОД`
3. Бот проверяет код в БД и привязывает `vk_id` к родителю

---

## 🔧 Системные функции

| Функция | Описание |
|---------|----------|
| `get_current_user(request, db)` | Извлекает пользователя из cookie сессии |
| `get_user_object(request, db)` | Возвращает объект ORM (ParentDB/CoachDB) |
| `calculate_age(birthdate)` | Вычисление возраста в годах |
| `get_group_enrollment_count(group_id, db)` | Актуальное количество детей в группе |

---

## 🗄️ Модели данных (основные)

| Модель | Описание |
|--------|----------|
| `ParentDB` | Родитель (email, пароль, vk_id, настройки) |
| `ChildDB` | Ребёнок (привязка к родителю, мед. данные) |
| `CoachDB` | Тренер |
| `GroupDB` | Группа (уровень, тренер, вместимость) |
| `TimeSlotDB` | Временной слот расписания |
| `TrainingDB` | Тренировка (дата, время, статус) |
| `EnrollmentDB` | Зачисление ребёнка в группу |
| `AttendanceDB` | Отметка посещаемости |
| `ApplicationDB` | Заявка на зачисление |
| `TransferRequestDB` | Запрос на перевод |
| `GalleryImageDB` | Фото галереи |

---

## 🚨 Известные особенности

1. **Пароли хранятся в открытом виде** - для тестового проекта норм, для продакшена нужен хэширование (bcrypt)
2. **Сессии в памяти** - при перезапуске сервера все сессии сбрасываются
3. **VK токен в коде** - нужно вынести в `.env`
4. **SQLite** - для продакшена рекомендуется PostgreSQL
```

---

## 🎤 ЧАСТЬ 2: ПЛАН ПРЕЗЕНТАЦИИ MAIN.PY

### Структура выступления (15-20 минут)

```markdown
# Презентация Pool CRM: main.py

## Временной план

1. **Введение** (2 мин) - что такое Pool CRM, проблемы, которые решает
2. **Архитектура** (3 мин) - общая структура, технологии
3. **Аутентификация и сессии** (2 мин) - как работает вход
4. **Ролевая модель** (2 мин) - Admin / Coach / Parent
5. **Ключевые модули** (5 мин) - дети, группы, тренировки, посещаемость
6. **VK интеграция** (2 мин) - как работает бот и привязка
7. **Демо** (3 мин) - живой показ
8. **Заключение** (1 мин) - итоги, планы развития
```

---

### Подробный сценарий

#### 1. ВВЕДЕНИЕ (2 минуты)

**Что сказать:**
> "Pool CRM - это система для управления плавательным бассейном. Основные проблемы, которые мы решаем: ручное заполнение таблиц, отсутствие коммуникации с родителями, сложность отслеживания посещаемости."

**Показать на слайде:**
```
Проблема → Решение
├── Excel таблицы → Централизованная БД
├── Звонки родителям → VK уведомления
├── Бумажные журналы → Электронная посещаемость
└── Долгий приём заявок → Онлайн-форма
```

---

#### 2. АРХИТЕКТУРА (3 минуты)

**Показать схему:**
```
┌─────────────────────────────────────────────────────────────┐
│                         Клиенты                              │
├─────────────┬─────────────┬─────────────┬───────────────────┤
│  Браузер    │  VK Bot     │  API (внешн)│                   │
└──────┬──────┴──────┬──────┴──────┬──────┴───────────────────┘
       │             │             │
       ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────┐
│                      FastAPI (main.py)                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │Auth     │ │Routes   │ │Templates│ │Session Manager  │   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────────┬────────┘   │
└───────┼───────────┼───────────┼───────────────┼─────────────┘
        │           │           │               │
        ▼           ▼           ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                    SQLAlchemy ORM                            │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │ParentDB │ │ChildDB  │ │GroupDB  │ │AttendanceDB     │   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘   │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────┐
│   SQLite    │
│ pool_crm.db │
└─────────────┘
```

**Ключевые технологии:**
- **FastAPI** - асинхронный веб-фреймворк
- **Jinja2** - шаблонизатор для HTML
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - лёгкая база данных

---

#### 3. АУТЕНТИФИКАЦИЯ (2 минуты)

**Показать код:**
```python
# Хранение сессий (в памяти)
sessions = {}

@app.post("/login")
async def login(email: str = Form(...), password: str = Form(...)):
    # Проверка админа
    if email == "admin@pool.ru" and password == "admin123":
        session_id = generate_session_id()
        sessions[session_id] = {"id": 1, "role": "admin", "name": "Админ"}
        
    # Проверка родителя/тренера в БД
    parent = db.query(ParentDB).filter(
        ParentDB.email == email,
        ParentDB.password == password
    ).first()
```

**Объяснить:**
- Сессии хранятся в словаре `sessions` (при перезапуске теряются)
- Cookie `session_id` хранится в браузере
- Три роли: admin, parent, coach
- Для продакшена нужно использовать JWT или Redis

---

#### 4. РОЛЕВАЯ МОДЕЛЬ (2 минуты)

**Показать таблицу:**

| Функция | Admin | Coach | Parent |
|---------|-------|-------|--------|
| Просмотр всех детей | ✅ | ❌ | только свои |
| Просмотр всех групп | ✅ | только свои | ✅ |
| Редактирование групп | ✅ | ❌ | ❌ |
| Отметка посещаемости | ✅ | ✅ | ❌ (только просмотр) |
| Создание заявки | ❌ | ❌ | ✅ |
| Одобрение заявок | ✅ | ❌ | ❌ |
| Переводы | одобряет | создаёт | ❌ |
| VK уведомления | ❌ | ❌ | получает |

**Показать код проверки прав:**
```python
def get_current_user(request: Request, db: Session):
    session_id = request.cookies.get("session_id")
    user_data = sessions.get(session_id)
    
    if user_data["role"] == "parent":
        return db.query(ParentDB).filter(id == user_data["id"]).first()
```

---

#### 5. КЛЮЧЕВЫЕ МОДУЛИ (5 минут)

##### 5.1. Управление детьми
```python
@app.get("/children")
async def children_list(request: Request, db: Session):
    user = get_current_user(request, db)
    
    # Родитель видит только своих детей
    if user["role"] == "parent":
        children = db.query(ChildDB).filter(parent_id == user["id"])
    
    # Для каждого ребёнка получаем текущую группу
    for child in children:
        enrollment = db.query(EnrollmentDB).filter(
            child_id == child.id,
            status == EnrollmentStatus.ACTIVE
        ).first()
```

##### 5.2. Посещаемость
```python
@app.post("/attendance/save")
async def save_attendance(training_id: int, request: Request):
    # Проверка прав (только тренер или админ)
    # Массовое обновление через form data
    for key, value in form_data.items():
        if key.startswith("status_"):
            child_id = int(key.split("_")[1])
            # Обновляем или создаём запись AttendanceDB
    
    # Отправка VK уведомления
    send_attendance_notification_vk(child_id, training_id, status)
```

##### 5.3. Заявки (самый сложный модуль)
```python
@app.get("/applications/{app_id}/approve")
async def approve_application(app_id: int):
    # 1. Меняем статус заявки
    # 2. Создаём родителя (если нет)
    # 3. Создаём ребёнка (если нет)
    # 4. Создаём зачисление в группу
```

---

#### 6. VK ИНТЕГРАЦИЯ (2 минуты)

**Как работает:**
```
1. Родитель генерирует код в профиле
2. Пишет боту "привязать 123456"
3. Бот проверяет код в БД
4. Привязывает VK ID к родителю
5. Родитель получает уведомления о пропусках
```

**Показать код генерации кода:**
```python
@app.post("/api/parents/{parent_id}/generate-vk-code")
async def generate_vk_code(parent_id: int, db: Session):
    code = ''.join(random.choices(string.digits, k=6))
    parent.vk_link_code = code
    parent.vk_code_expires_at = datetime.now() + timedelta(minutes=10)
    db.commit()
    return {"code": code}
```

---

#### 7. ДЕМО (3 минуты)

**Показать live:**
1. **Вход админом** → дашборд со статистикой
2. **Просмотр заявок** → одобрить новую заявку
3. **Создание группы** → добавить новую группу
4. **Вход тренером** → отметить посещаемость
5. **Вход родителем** → посмотреть детей, сгенерировать VK код
6. **Галерея** → загрузить фото

---

#### 8. ЗАКЛЮЧЕНИЕ (1 минута)

**Что сделано:**
- ✅ Полноценная CRM с 3 ролями
- ✅ VK бот с 6 командами и уведомлениями
- ✅ Фотогалерея с миниатюрами
- ✅ 15+ HTML шаблонов
- ✅ 10+ эндпоинтов API

**Планы развития:**
- 🔜 Миграция на PostgreSQL
- 🔜 Хэширование паролей (bcrypt)
- 🔜 JWT аутентификация
- 🔜 Взаимодействие с электронной почтой

---

### ВОПРОСЫ ДЛЯ КОМАНДЫ

| Вопрос | Ответ |
|--------|-------|
| Почему SQLite? | Для простоты развёртывания, можно заменить на PostgreSQL |
| Как масштабировать? | Вынести сессии в Redis, БД в PostgreSQL |
| Как добавить новую роль? | Добавить enum в `UserRole`, новые проверки в `get_current_user` |
| Почему `api_routes.py` не используется? | Это REST API для внешних систем, пока не требуется |
